services:
    postgres:
        image: postgres:18-alpine
        container_name: db
        env_file:
            - .env

        ports:
            - "5432:5432"
        volumes:
            - ./data:/var/lib/postgresql/data

volumes:
    postgres_data:
